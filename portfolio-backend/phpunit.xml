<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <!-- Four suites, each runnable on its own. Unit is the fast inner loop and
             touches no database; Feature covers HTTP and multi-component
             integration against the disposable test database; Regression holds one
             test per historically-fixed bug, named after the bug it guards;
             Contract pins the API payload shapes the Next.js apps depend on.

             Order matters: PHPUnit runs suites in declaration order, so a broken
             pure-logic assertion surfaces before the slower database work. -->
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Regression">
            <directory>tests/Regression</directory>
        </testsuite>
        <testsuite name="Contract">
            <directory>tests/Contract</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- MySQL, not the Laravel-default in-memory SQLite: this environment's
             PHP has only pdo_mysql (no pdo_sqlite), so ":memory:" fails with
             "could not find driver". Create the database once with:
               CREATE DATABASE portfolio_backend_test
                 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
             RefreshDatabase migrates it per test run, so it stays disposable and
             never touches the development database. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="portfolio_backend_test"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
